// This acts as our in-memory user database
// In real apps, this would be a real database like MongoDB
let users = [];
// We start with empty array — users must register first

module.exports = users;